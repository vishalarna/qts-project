import {NgModule } from '@angular/core';
import {declarations, imports, providers, bootstrap } from './app.module.meta'


@NgModule({
  declarations:declarations,
  imports: imports,
  providers:providers,
  bootstrap:bootstrap
})
export class AppModule {}
