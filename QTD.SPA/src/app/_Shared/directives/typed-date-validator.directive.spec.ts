import { TypedDateValidatorDirective } from './typed-date-validator.directive';

describe('TypedDateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TypedDateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
