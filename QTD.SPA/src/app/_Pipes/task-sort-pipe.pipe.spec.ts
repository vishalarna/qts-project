import { TaskSortPipePipe } from './task-sort-pipe.pipe';

describe('TaskSortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TaskSortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
